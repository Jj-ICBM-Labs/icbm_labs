export type researchTypes = {
    research: string;
    startDate: string;
    endDate: string;
};

export type welfareTypes = {
    title: string;
    description?: string;
    etc?: string[];
};

export type wantedTypes = {
    title: string;
    description: string[];
};